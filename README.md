My machine learning project focuses on building a neural network–based image generator using Python and PyTorch. The model is designed to learn patterns from training data and generate new images from random noise, demonstrating how generative models work. It uses a lightweight architecture to run efficiently on low-resource systems while still producing meaningful outputs. This project highlights key concepts such as training loops, loss optimization, and neural network design, making it a practical introduction to image generation in machine learning
